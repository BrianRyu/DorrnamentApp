<h1> <%= @player.name %> </h1>

<h3>Player Info</h3>
<p> Date Of Birth: <%= @player.pretty_birth_date %> </p>
<p> Gender: <%= @player.gender %> </p>
<p> Start Date of AVP: <%= @player.pretty_start_date if !!@player.start_date %> </p>
<p> <%= "Email: #{@player.email}" if @player.id == session[:player_id]%> </p>
<%= image_tag "#{@player.img_url}", width: '500', class: 'right_img' %>

<%= button_to "Edit Information", {:controller => :players,
  :action => 'edit'}, {:method => :get, class: 'button'} if admin or is_self %>

<h3>Teams</h3>
<ul>
  <% @player.teams.uniq.each do |team| %>
  <li><%= link_to team.name, team %></li>
  <% end %>
</ul>
