<h1>Edit Player Info</h1>
<%= render 'form' %>
<%= button_to "Delete Player", {:controller => :players,
  :action => 'destroy', :id => @player.id}, {:method => :delete,
  data: {confirm: "This will delete the player. Are you sure?"},
  class: 'button'} %>
<%= render 'errors' %>
